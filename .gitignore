node_modules/

# Android build outputs (gerados pelo Gradle, não precisam ir pro git)
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/captures/
android/.idea/
android/app/release/
android/app/debug/
*.keystore
*.jks

# Lista de tarefas pessoal, não é código do projeto
fazer.json

# Funções Supabase — coladas manualmente no dashboard, não fazem parte do deploy pelo git
supabase/functions/

# www/ é 100% gerado pelo "npm run sync" — copia index.html, voz/, icones/, pdfjs/ pro www/
# e depois roda "npx cap copy android". Não precisa versionar duplicata gerada.
www/

# vozes/ — pasta de rascunho de áudio local, não é asset do app
vozes/

# Dados de sessão do Claude Code (editor AI)
.claude/

# Backup manual do index.html, não é pra subir
backup/

# Notas do Claude Code sobre o projeto, uso interno, não é pra subir
CLAUDE.md

# Bot de Discord (discord-bot/) — segredos nunca vão pro git
discord-bot/.env

# Cache local da CLI do Supabase (project-ref linkado, versão) — não é segredo, mas é cache de máquina
supabase/.temp/

# Credenciais/chaves de API coletadas durante a migração do Supabase — nunca sobe pro git
.env
.env.*
